import streamlit as st

# Create a button to clear the content
clear_button = st.button("Clear Content")

# JavaScript code to clear the content
javascript_code = """
document.body.innerHTML = '';
"""

# Check if the button is clicked
if clear_button:
    # Execute JavaScript code to clear the content
    st.write(javascript_code, unsafe_allow_html=True)
